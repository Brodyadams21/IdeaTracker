// Firebase Configuration
// 
// To set up Firebase for this app:
// 1. Go to https://console.firebase.google.com/
// 2. Create a new project or select existing one
// 3. Add an Android app with package name from android/app/build.gradle
// 4. Add an iOS app with bundle ID from ios/IdeaTrackerApp/Info.plist
// 5. Download google-services.json (Android) and GoogleService-Info.plist (iOS)
// 6. Place them in android/app/ and ios/IdeaTrackerApp/ respectively
// 7. Enable Authentication > Sign-in method > Anonymous
// 8. Enable Firestore Database > Create database > Start in test mode

// The app will work with anonymous authentication by default
// No additional configuration needed for basic functionality

export const firebaseConfig = {
  // Your Firebase config will be automatically loaded from the native config files
  // This file is just for documentation and future custom configuration
};

export default firebaseConfig;
